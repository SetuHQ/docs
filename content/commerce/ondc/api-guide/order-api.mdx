---
sidebar_title: Order API
page_title: Order API
order: 5
visible_in_sidebar: true
---

## Order APIs

The **Order** resource is used to define an ONDC-routed order placed to a single **Storefront**.

The **Order APIs** can be used to–

1. Get orders for a storefront <Badge>Mandatory</Badge>
2. Update orders for a storefront <Badge>Mandatory</Badge>

### Get orders for a storefront

**Sample Request**

<table>
  <tr>
    <th>Method</th>
    <td>
      <code>GET</code>
    </td>
  </tr>
  <tr>
    <th>URL</th>
    <td>
      <code>/api/storefronts/{"storefront_id"}/orders</code>
    </td>
  </tr>
</table>

**Sample Response**

<CodeBlockWithCopy>{`
{
    [
        {
            "order_id": "string", 
            "order_state": "string",
        }, 
        {
            "order_id": "string", 
            "order_state": "string",
        }
    ]
}
`}</CodeBlockWithCopy>

### Update orders for a storefront

**Sample Request**

<table>
  <tr>
    <th>Method</th>
    <td>
      <code>PUT</code>
    </td>
  </tr>
  <tr>
    <th>URL</th>
    <td>
      <code>/api/storefronts/{"storefront_id"}/orders</code>
    </td>
  </tr>

</table>
<br />

**Sample Request Body**

<CodeBlockWithCopy>
  {`
{
	[
		{
			"order_id": "string", 
			"order_state": "string",
		}, 
		{
			"order_id": "string", 
			"order_state": "string",
		}
	]
}
    `}
</CodeBlockWithCopy>

<NextPage
  info={{
    description: "Jump to API Reference",
    slug: "/commerce/ondc/api-reference",
    title: "API Reference",
  }}
/>

<WasPageHelpful />
